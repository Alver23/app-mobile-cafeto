// Dependencies
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	mainContainer: {
		flex: 1,
		margin: 12,
	},
	buttonContainer: {
		marginTop: 4,
		marginBottom: 4,
		alignItems: 'center',
	},
});
